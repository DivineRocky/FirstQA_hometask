using System;

namespace ArrayOperations__HW5_
{
    public class ArrayOperations
    {
        private int _lowerBound;
        private int _elementQuantity;

        public ArrayOperations(int lowerBound, int elementQuantity)
        {
            _lowerBound = lowerBound;
            _elementQuantity = elementQuantity;
        }
        public int[] CalculateArrayBoundsAndLength(int[] array)
        {
            lowerBound = index;
            upperBound = index + _elementQuantity - 1;

            for (int index = 0; index < array.Length; index++)

                return 1;
        }
    }
}
